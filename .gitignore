/target
.idea/
.vscode/

# Runtime artifacts of the dev server
/luradb.wal
/luradb.db
/luradb.vlog
/luradb_sstables
/luradb_rel.wal
/luradb_rel.vlog
/luradb_rel_sstables

# Benchmarks: local development tooling, not part of the published product
/benches/

# CycloneDX SBOM output (cargo cyclonedx); a release asset, not tracked here
/*.cdx.json

# Private meta part (separate repo) and local tooling
/concepts/
/AGENTS.md
/.claude/
/tools/
