sonar.projectKey=hwein_luradb
sonar.organization=hwein

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=luradb
sonar.projectVersion=1.0

# Scan scope: production code only — benches/ and tests/ stay out of the analysis.
sonar.sources=src

sonar.sourceEncoding=UTF-8
