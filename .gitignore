/target
.idea/
.vscode/

# Runtime artifacts of the dev server
/luradb.wal
/luradb.db
/luradb.vlog
/luradb_sstables

# Benchmark reports (cargo bench)
/benches/results/

# CycloneDX SBOM output (cargo cyclonedx); a release asset, not tracked here
/*.cdx.json

# Private meta part (separate repo) and local tooling
/concepts/
/AGENTS.md
/.claude/
/tools/
